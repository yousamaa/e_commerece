<div class="panel-body">
  <%= form_with model: product,local: true do |form| %>
    <%= render 'error', product: product %>
    <div class="field form-group">
      <br>
      <%= form.text_field :name, autofocus: true, autocomplete: "name", class: "form-control", placeholder: "Name" %>
    </div>

    <div class="field form-group">
      <br />
      <%= form.number_field :price, autofocus: true, autocomplete: "price", class: "form-control", placeholder: "Price" %>
    </div>

    <div class="field form-group">
      <br>
      <%= form.file_field :images, multiple: true %>
    </div>

    <div class="actions">
      <br>
      <%= form.submit class: "btn btn-primary" %>
    </div>
  <% end %>
</div>